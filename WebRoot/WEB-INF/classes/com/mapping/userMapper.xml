<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserMapper">
    <!-- 解决表名与字段不匹配 -->
    <resultMap type="User" id="userResultMap">
        <id property="user_id" column="user_id"/>
        <result property="user_name" column="user_name"/>
        <result property="password" column="password"/>
        <result property="e_mail" column="e_mail"/>
        <result property="description" column="description"/>
                
    </resultMap>
    
    <sql id="UserColumn">
		 user_name, password ,e_mail ,description
	</sql>
    
    <select id="findAllUser" resultMap="userResultMap" resultType="User">
        select * from tb_user
    </select>
    
     <select id="selectByPrimaryKey" resultMap="userResultMap" resultType="User">
        select
		<include refid="UserColumn" />
		from tb_user
		where user_id = #{userId,jdbcType=INTEGER}
    </select>
    
    <select id="getUsernameAndPassword"  resultType="Integer">
        select count(*) from tb_user where user_name = #{username} and password = #{password}
		
    </select>
    
     <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
		insert tb_user (<include refid="UserColumn"/>) values (#{user_name},#{password},#{e_mail},#{description})
	</insert>
</mapper>